ポインタ：　アドレスを格納するための変数

メモリ：　記憶領域

アドレスを引っ張る：　変数の前に&(アドレス演算子)

アドレスが指し示している領域のデータを引っ張る：　変数の前に*(間接演算子)

&で先頭アドレスが入る

ポインタ変数は値が何であれ4つ分の領域を確保

変数をそのまま渡す：　値渡し

アドレスを渡す：　参照渡し

構造体：　複数の異なる型をまとめて作られた型

構造体の型名を構造体タグ名という

(*構造体ポインタ変数名).要素名 = 構造体ポインタ変数名->要素名

strcmp：　等しいならば0、じゃなければ1